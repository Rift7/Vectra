# Vectra API

FastAPI backend for vectorization, processing, optimization, and G-code generation.

## Run (placeholder)
- Install requirements
- Start: `uvicorn main:app --reload --port 8000`

## Data
Local data stored in `apps/api/data/projects`.
